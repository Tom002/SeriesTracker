apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "seriestracker.fullname" . }}-test-connection"
  labels:
{{ include "seriestracker.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "seriestracker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
